# Engineering Tasklist (v0.40.0 Prototype)

## MISSION 1

[IMMEDIATE] [CRITICAL] Cryptographic Integrity & Security Blocks

- [ ] **Task 1.1:** Refactor password hashing in `src/services/crypto.js` to use `crypto.scrypt` (16-byte salt, stored as `salt_hex:hash_hex`).
- [ ] **Task 1.2:** Update `verifyPassword()` to use `crypto.timingSafeEqual()` to mitigate timing attacks.
- [ ] **Task 1.3:** Fix typo in `encryptEmail()` (change `"hec"` to `"hex"`).
- [ ] **Task 1.4:** Fix bug in `decryptEmail()` (assign `authTagHex` correctly instead of using `ivHex`).
- [ ] **Task 1.5:** Fix `hashKey()` reference error (replace undefined method with `crypto.randomBytes()`).
- [ ] **Task 3.1:** Implement active user cache using a `Set` and a disconnect timer using a `Map` in `handlers/account.js`.
- [ ] **Task 3.2:** Enforce 10-second grace period on disconnects (Anti-F5 spam); cancel timers upon successful reconnection.
- [ ] **Task 3.6:** Join verified connections into private Socket.io rooms: `socket.join('account:...')`.

## Priority 1: Metric Scaffolding & State Tracking (Medium)

- [ ] **Task 2.1:** Connect to `server.db` via `MAGPIE_DATABASE.sync` in `handlers/session.js`.
- [ ] **Task 2.2:** Define and initialize the `game_metrics` table schema.
- [ ] **Task 2.3:** Pre-populate base metrics (`visitor_count`, `peak_concurrent_players`).
- [ ] **Task 2.4:** Write helper queries for metric reads and atomic increments.
- [ ] **Task 3.3:** On new user connection: increment `visitor_count` and broadcast `counter-update` via Socket.io.
- [ ] **Task 3.4:** On returning user connection: skip DB increment and emit current count.
- [ ] **Task 3.5:** Support multi-device access state via the `server.session.players` map.

## Priority 2: UI Presentation Layer (Low)

- [ ] **Task 4.1:** Build `RetroOdometer` JavaScript class in `public/main.js`.
- [ ] **Task 4.2:** Implement `localStorage.getItem("has_been_counted")` check on the client to avoid double-counting.
- [ ] **Task 4.3:** Create 7-digit slot HTML structure with vertical ribbon (0-9 columns).
- [ ] **Task 4.4:** Animate the odometer on `counter-update` events with smooth CSS transitions in `public/home.css`.
- [ ] **Task 4.5:** Apply retro pixel-art rendering using `image-rendering: pixelated` across frontend assets.
